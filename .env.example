# AI API Keys
DASHSCOPE_API_KEY=your_dashscope_api_key_here

# LLM Model
LLM_MODEL=qwen-max

# Database (使用Docker Compose时不需要修改)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/booklist_video

# Redis (使用Docker Compose时不需要修改)
REDIS_URL=redis://localhost:6379/0

# MinIO (使用Docker Compose时不需要修改)
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_NAME=booklist-video
